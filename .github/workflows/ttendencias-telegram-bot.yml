name: TTendencias Telegram Listener (retirado)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ttendencias-telegram-bot.yml"

concurrency:
  group: ttendencias-telegram-listener
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Listener antiguo retirado; usa ttendencias-listener.yml"
