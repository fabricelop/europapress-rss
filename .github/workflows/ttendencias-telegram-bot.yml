name: TTendencias Telegram Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"
  push:
    branches: [main]
    paths:
      - "trends/recent.json"
      - "trends/telegram-manual-explained.json"

permissions:
  contents: write

concurrency:
  group: ttendencias-telegram-bot
  cancel-in-progress: false

jobs:
  bot:
    runs-on: ubuntu-latest
    timeout-minutes: 59
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Git
        run: |
          git config user.name "ttendencias-bot"
          git config user.email "actions@users.noreply.github.com"

      - name: Sincronizar panel
        env:
          TTENDENCIAS_BOT_TOKEN: ${{ secrets.TTENDENCIAS_BOT_TOKEN }}
        run: python3 trends/telegram_bot.py sync

      - name: Escuchar botones
        if: github.event_name != 'push'
        env:
          TTENDENCIAS_BOT_TOKEN: ${{ secrets.TTENDENCIAS_BOT_TOKEN }}
          TTENDENCIAS_LISTEN_SECONDS: "3300"
        run: python3 trends/telegram_bot.py listen

      - name: Guardar estado del bot
        if: always()
        run: |
          set -e
          git add trends/telegram-bot-state.json trends/telegram-manual-explained.json
          if git diff --cached --quiet; then
            echo "Sin cambios de estado."
            exit 0
          fi
          git commit -m "Actualizar estado bot TTendencias"
          for intento in 1 2 3 4; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then
              exit 0
            fi
            sleep $((intento * 2))
          done
          exit 1
